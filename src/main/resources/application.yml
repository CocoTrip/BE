spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  profiles:
    include: oauth

jwt:
  issuer: yoseb95@gmail.com
  secret_key: teamprojectsecret